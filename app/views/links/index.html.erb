<p id="notice"><%= notice %></p>

<div class="page-header">
              <h1>Linkster  <small>Your favorite link website</small></h1>
            </div>
<div class="list-group">
  <% @links.each do |link| %>
  <%= link_to link.title, link.url, class: "list-group-item" %>
  <% end %>
</div>